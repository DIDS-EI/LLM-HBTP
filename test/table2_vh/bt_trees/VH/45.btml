selector
    cond IsIn(wine,fridge)
    sequence
        cond IsRightHolding(self,wine)
        selector
            sequence
                cond IsNear(self,fridge)
                selector
                    sequence
                        cond IsOpen(fridge)
                        act RightPutIn(wine,fridge)
                    sequence
                        cond IsClose(fridge)
                        cond IsLeftHandEmpty(self)
                        act Open(fridge)
            sequence
                cond IsClose(fridge)
                cond IsLeftHandEmpty(self)
                cond IsStanding(self)
                act Walk(fridge)
    sequence
        cond IsClose(fridge)
        cond IsLeftHandEmpty(self)
        cond IsRightHandEmpty(self)
        cond IsStanding(self)
        selector
            sequence
                cond IsNear(self,wine)
                act RightGrab(wine)
            act Walk(wine)
